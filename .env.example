# Cortex — HackRice
# TRIBE v2 runs ONLY on Kaggle GPU T4x2 — never on this laptop.
# After the notebook finishes, drop the downloaded JSON into data/activations/

TRIBE_V2_ENDPOINT=
CORTEX_ACTIVATION_DIR=./data/activations

# Used inside the Kaggle notebook (Add-ons → Secrets), not required locally for demo
HF_TOKEN=

GEMINI_API_KEY=
ELEVENLABS_API_KEY=

TIGERDATA_CONNECTION_STRING=
CONSENT_SMS_PROVIDER_KEY=
